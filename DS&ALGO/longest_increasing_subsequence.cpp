#include <bits/stdc++.h>
using namespace std;

#define ll long long
#define FASTIO  \
    cin.tie(0); \
    ios_base::sync_with_stdio(false);
#define TC while (t--)
const int MOD = 1e9 + 7;

int main()
{
    FASTIO
    ll t;
    cin >> t;
    TC
    {
        ll n;
        cin >> n;
        ll a[n];
        for (int i = 0; i < n; i++)
        {
            cin >> x[i];
        }
        ll prev[n] = {1};
        for (int i = 0; i < n; i++)
        {
            ll subs[]
        }
    }
}