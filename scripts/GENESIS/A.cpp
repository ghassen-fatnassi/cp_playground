#include <bits/stdc++.h>
using namespace std;

#define ll long long
#define FASTIO  \
    cin.tie(0); \
    ios_base::sync_with_stdio(false);
#define TC while (t--)
const int MOD = 1e9 + 7;

pair(int, int) visited[i][j];

void bfs(ll currx, ll curry)
{
    if (visited[currx][curry] == 1)
    {
        }
}

int main()
{
    FASTIO
    ll grid[n][n];
    ll n;
    cin >> n;
    for (int i = 0; i < n; i++)
    {
        for (int j = 0; j < n; j++)
        {
            cin >> a[i][j];
        }
    }
    return 0;
}